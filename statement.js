// statements are instructions that are executed by the browser . A statement can be simple or complex operation that performs an action.

// Example
console.log('Hello world');
const str = 'string'
const a = 10;
const b = 20;
// all are statements 